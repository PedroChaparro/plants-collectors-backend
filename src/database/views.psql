DROP VIEW IF EXISTS PLANTS_WITH_POPULATED_USER;
CREATE OR REPLACE VIEW PLANTS_WITH_POPULATED_USER AS
  SELECT PLANTS.id "plant_id", PLANTS.name "plant_name", USERS.username "owner_username"
  FROM PLANTS LEFT JOIN USERS ON PLANTS.seller_id = USERS.id;